<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="24dp"
    app:cardCornerRadius="24dp"
    app:cardElevation="8dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- TÍTULO -->
        <TextView
            android:id="@+id/textTituloTermos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Termos e Condições"
            android:textColor="#1B3434"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="16dp"/>

        <!-- SCROLLVIEW DO TEXTO -->
        <androidx.core.widget.NestedScrollView
            android:id="@+id/scrollTermos"
            android:layout_width="match_parent"
            android:layout_height="220dp"
            android:background="#F5F7F7"
            android:padding="12dp">

            <TextView
                android:id="@+id/textCorpoTermos"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#415E5E"
                android:textSize="14sp"
                android:lineSpacingMultiplier="1.2" />
        </androidx.core.widget.NestedScrollView>

        <!-- CHECKBOX (Inicia invisível ou desabilitado) -->
        <CheckBox
            android:id="@+id/checkAceitarTermos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Li e aceito as condições de empréstimo"
            android:textColor="#1B3434"
            android:textSize="14sp"
            android:buttonTint="@color/biblio_blue"
            android:visibility="gone" /> <!-- 🌟 Só aparece ao chegar no fim -->

        <!-- BOTÕES DE AÇÃO -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="16dp"
            android:weightSum="2">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnRecusarTermos"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="0dp"
                android:layout_height="50dp"
                android:layout_weight="1"
                android:text="Recusar"
                android:textColor="@color/biblio_red"
                android:textAllCaps="false"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnConfirmarTermos"
                android:layout_width="0dp"
                android:layout_height="50dp"
                android:layout_weight="1"
                android:text="Avançar"
                android:textAllCaps="false"
                android:enabled="false"
                app:backgroundTint="@color/biblio_blue"
                android:textColor="@color/white"
                app:cornerRadius="25dp"/>
        </LinearLayout>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>